# 当前执行计划

更新时间：2026-07-11

## 目标

对项目执行全库代码审查与底层清扫，删除迁移前对话数据和无用兼容代码，确立 SQLite 单向权威，重点加固前端操作状态机与对话生命周期，完成全量验收后合并 `main` 并推送 GitHub。

## 已完成

- 删除旧对话迁移、页面 OCR runner、旧 persona profile/topic planner 及对应测试。
- 删除旧白名单命令、配置别名和文件契约，统一使用 accepted channel 准入。
- 通道、账本、session、确认队列、发送审计、任务与侧栏状态改为 SQLite 单向权威，不再从文件投影回填。
- 修复通道删除、历史清理、只读内容寻址附件清理和配置并发写入。
- 加固前端刷新排队、作用域锁、任务串行、保存期间编辑保留、关闭面板竞态、历史重置冻结、超时与非法 JSON 处理。
- 阻断未知 OCR/窗口快照；已注册通道中的不可信快照仅作只读上下文。
- 历史上下文不再触发 session 重置或主动补答；批内重置命令在后续消息前生效。
- 主动回复增加跨进程 tick 锁与账本水位原子提交，避免并发重复回复和陈旧回复落账。
- 自发消息回显仅匹配实际投递状态，不再吞掉 dry-run 或后续手动同文案。
- 群聊冷却改为回复成功落账后提交，生成失败不污染冷却窗口。
- 删除旧 `window_bindings.json` 前台绑定兼容支路；侧栏可见性不再把迁移前绑定文件当作通道信任证据。
- 修正 preflight 的存储权威声明，并删除仅服务旧测试 stub 的 LLM workload 与前端历史摘要兼容层。

## 待最终验收

- [x] 清理实际 `data` 中的旧白名单文件、旧侧栏副本和迁移前运行数据，同时保留配置与完整 `send_bridge` 证据链。
- [x] 核对 SQLite 表、索引、版本、完整性与空历史行数输出。
- [x] 完成后端全量测试和静态检查。
- [x] 完成桌面与窄屏浏览器交互验收。
- [x] 确认真实发送关闭并复核 `send_bridge` 哈希。
- [ ] 提交功能分支，合并到 `main`，推送 GitHub。

## 完成定义

计划审计无残留；代码、数据和浏览器验收全部通过；没有项目进程遗留；`main` 与 GitHub 远端包含同一最终提交。
