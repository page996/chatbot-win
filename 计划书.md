# 计划书

## 2026-07-10 20:20 SQLite 权威存储、投递 fail-closed 与空历史验收

1. 执行口径：
   - 已先提交 checkpoint `c28f554`，随后在 `weflow-stability-observability` 分支继续收尾。
   - 部署不迁移既有通道、ledger、session 文件；用户清空历史后直接以新数据库链路启动。
   - 全程保持 `send_enabled=false`，不做真实微信发送 probe，不向真实 outbox/acks 写测试记录。
   - `data/send_bridge` 的 outbox、acks、synced marker、accepted reverify marker 和旧 worker lock 全部保留。
2. 投递安全收敛：
   - 未登记群聊、未知/非好友私聊、receiver 被替换时，均在 wire 前 fail closed。
   - outbox receiver 必须和通道权威库中的 wxid/roomid 精确一致。
   - worker 每次 retry 前重新检查 `send_enabled`、driver/backend、receiver 与通道授权；第一次失败后关闭发送或撤权，第二次不会触线。
   - `accepted_unverified` 可能已经真实投递，不允许自动或手工重投，只等待回读确认。
   - `filehelper` 只保留 `conversation_id=filehelper` 的明确自发送例外。
3. 三个 SQLite 权威库：
   - `conversation_channels.sqlite`
     - `channel_registry_meta`、`conversation_channels`。
     - `conversation_id` 主键，`idx_conversation_channels_updated` 更新时间索引。
     - `channel.json`、`index.json` 为可读投影。
   - `conversation_ledger.sqlite`
     - `ledger_meta`、`ledger_conversations`、`ledger_entries`。
     - `(conversation_id, entry_id)` 主键；`(conversation_id, sequence)` 唯一索引保证通道内线性顺序；另有 session/order 索引。
     - `messages.jsonl`、`conversation.md` 为可读投影。
   - `conversation_sessions.sqlite`
     - `session_meta`、`conversation_session_states`、`conversation_session_events`。
     - state 以 `conversation_id` 为主键，reset event 使用自增 `event_id` 并按 conversation/event 建索引。
     - `state.json`、`events.jsonl` 为可读投影。
   - DB-only 读取会重新生成缺失投影；Agent 枚举、bridge ack/requeue、task 修复等消费者均从数据库枚举，不依赖目录扫描。
4. 空历史和数据库实测：
   - 在隔离临时 `data` 中执行空启动、首次通道/session/ledger 写入、删除全部六类投影、数据库恢复、清历史和空库复开。
   - 首次写入后三库 `schema_version=1`、`integrity_check=ok`，各业务表各 1 行。
   - 删除投影后，通道标题、ledger sequence、当前 session 和 reset event 均从 SQLite 恢复，六类投影全部重建。
   - 清历史删除三库及 `-wal/-shm`；复开后旧通道、消息、session/reset event 均为 0，不会从残留文件复活。
   - `storage-status` 只读输出 integrity、schema version、表、列、索引、行数和缺失表；缺失数据库不会仅因审计被创建。
5. 附件内容寻址：
   - 原件按 SHA-256 写入 `file_workspace/_blobs/<prefix>/<digest>`。
   - staged 原件优先 hardlink，失败时 copy；blob/staged 原件设为只读，解析前校验摘要并可从 blob 恢复。
   - cleanup 与 staging 共用互斥边界，只回收无 manifest 引用 blob；容量按唯一文件 identity 统计，不重复计算 hardlink。
   - 修复异步分析在任意临时 workspace 父目录误建 `conversation_ledger.sqlite` 的问题；只有正式 `<data>/file_workspace` 才回填 ledger。
6. 残留与工具清理：
   - 删除废弃 `conversation/context_store.py`。
   - 删除 fake 搜索工具 `tools/search/external_search.py`、`model_relevance_filter.py`；正式入口保持 `web.search`、`web.fetch`。
   - 历史清理范围补齐三个权威库及 WAL/SHM，同时继续保留完整 send bridge 证据链。
7. sidebar 与数据库可观测性：
   - 存储边界页优先展示完整 `database_contracts`，不再因 9000 字符组件预览截断三库合同。
   - 组件明细单独限长，三库 schema、integrity、表/列/索引/行数始终可见。
   - 增加空 favicon，消除浏览器 404 控制台噪声。
   - Playwright 验收 `1440x1000` 与 `390x844`，遍历概览、发送、WeFlow、任务与资源、历史、诊断六页：无横向溢出、控件重叠、文字裁切或浏览器错误。
8. 自动化验证：
   - `python -m unittest discover -s tests`：840 tests OK，486.489s。
   - sidebar 专项：170 tests OK。
   - `tests.test_file_analysis`：12 tests OK；`tests.test_file_workspace`：36 tests OK。
   - 原 Windows 异步 ledger 锁竞态场景连续运行 20 次通过。
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`：通过。
   - `git diff --check`：无 whitespace error，仅既有 CRLF warning。
9. 真实运行态：
   - `data/config.json` 中 `send_enabled=false`。
   - 旧 send bridge worker PID `15232` 已停止；`.bridge_worker.lock` 作为历史证据保留，不视为存活状态。
   - 真实 `data` 未因数据库结构审计创建 `conversation_channels.sqlite`、`conversation_ledger.sqlite`、`conversation_sessions.sqlite`。
   - 本轮没有启动 Agent/WeFlow/send bridge worker，没有真实投递，也没有清理用户历史。

## 2026-07-10 10:05 全库长任务加固、sidebar 契约复核与对话质量补强

1. 本轮目标：
   - 承接《后续完整计划书.md》，按长期任务流程继续推进“全库模块扫描、残留清理、并发稳定性、sidebar 前后端契约、对话聚合和回复质量”。
   - 不只补局部问题，而是把本轮新增逻辑纳入可回归测试和真实数据巡检。
2. 本轮扫描与清理：
   - 扫描 `app/scripts/tests` 中的 `TODO/FIXME/HACK/NotImplemented/WCF/wcferry/WeChatFerry/wcf-callback` 残留，未发现可行动命中。
   - 清理工作区无效临时残留：`$out/`、`injector.log`。
   - 未清理历史数据，未删除 `data/send_bridge/outbox.jsonl`、`acks.jsonl`、`synced_acks.json`、`accepted_reverify.json`、`.bridge_worker.lock` 等发送桥证据。
3. 本轮修复与加固：
   - `app/personal_wechat_bot/conversation/prompt_builder.py`
     - 强化底部/最新语境优先。
     - 明确旧 summary 只作为背景，不逐条追答旧历史。
     - 强化群聊聚合，不机械按人逐条回复。
     - 文件未解析/不可读时禁止虚构内容。
   - `app/personal_wechat_bot/control/sidebar_api.py`
     - 新增 Agent proactive reply 重复保护：生成的主动回复若与近期 assistant/agent 文本高度相似，则跳过并返回 `repeat_guard_recent_assistant`。
     - 补充 Agent tick 并发串行化回归，防止多个并发 `/api/agent/tick` 同时进入同一轮 runner。
     - 新增对话主题生命周期派生字段 `topic_lifecycle` 与 `topic_lifecycle_counts`，结构化标记 `open/responded/sent/closed/reopened`，为后续“旧主题不主动重开”和前端展示提供稳定状态。
   - `tests/test_sidebar_server.py`
     - 已有“所有 HTML button 必须绑定事件或归属 form”的静态回归保持通过。
     - 新增“前端 `api(...)` 调用必须被 `sidebar_server.py` 路由覆盖”的静态契约测试，覆盖静态路径和 queue/channel/runtime-card/WeFlow/Agent 动态路径。
4. sidebar 前后端契约复核：
   - 前端静态 API 调用共 26 个静态路径、8 个模板路径，均有后端 exact route 或显式动态 route 形状承接。
   - 本轮覆盖的高风险交互包括：清审计、清历史、任务总台、控制保存、bridge ack/retry、通道状态、通道删除/测试回复/测试文件、诊断导出、runtime probe、resource audit、Agent tick/start/stop、WeFlow health/pull/backfill/start/stop/deps/install、model config/key pool。
5. 真实运行态修复：
   - 巡检时发现 send bridge worker 旧 pid `2608` 已死亡，`config_status=not_running`，但任务/队列/bridge backlog 均为 0。
   - 已用正式入口 `python scripts/send_bridge_worker.py --data-dir data --interval 2 --strict-data-dir` 隐藏后台启动外部 worker。
   - 最终状态恢复为 external process，pid `15232`，`pid_alive=true`，`lock_alive=true`，backend=`wechat_native_http`，`config_status=matched`，心跳新鲜。
6. 验证：
   - 残留扫描：无 TODO/FIXME/HACK/NotImplemented/WCF/wcferry 命中。
   - 聚焦新增测试：
     - `test_prompt_strengthens_bottom_context_repeat_and_group_guidance`
     - `test_agent_repeat_guard_blocks_highly_similar_proactive_reply`
     - `test_sidebar_agent_tick_is_serialized_across_concurrent_requests`
     - `test_sidebar_buttons_are_bound_or_form_owned`
     - `test_sidebar_api_calls_are_routed_by_server`
     - `test_agent_snapshot_reports_topic_lifecycle_statuses`
   - `python -m unittest tests.test_send_bridge_worker tests.test_send_commands tests.test_task_manager tests.test_send_backends tests.test_send_readiness tests.test_bridge_send -v`：168 tests OK。
   - `python -m unittest tests.test_conversation_engine tests.test_sidebar_api tests.test_sidebar_server tests.test_conversation_ledger tests.test_memory_maintainer tests.test_channel_state_store -v`：189 tests OK。
   - `python -m unittest tests.test_conversation_engine tests.test_sidebar_api tests.test_sidebar_server -v`：146 tests OK。
   - `python -m unittest discover -s tests`：765 tests OK。
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`：通过。
   - `python -m app.personal_wechat_bot.main --data-dir data diagnostics-export --limit 5 --no-persist`：OK，未持久化诊断文件，敏感字段脱敏。
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK，`send_backend=wechat_native_http`，native `IsLogin=1`，worker `matched`，`pending_count=0`。
   - `git diff --check`：无 whitespace error，仅既有 CRLF warning。
7. 真实数据状态：
   - task counts：`active=0`，`running=0`，`queued=0`，`waiting=0`，`blocked=0`，`total=95`。
   - confirm counts：`pending=0`，`approved=0`，`queued_to_bridge=0`，`accepted=0`，`sent=0`，`failed=3`，`rejected=0`。
   - bridge：`pending_count=0`，`active_unverified_count=0`。
   - worker：external process `pid=15232`，`pid_alive=true`，`lock_alive=true`，`backend_name=wechat_native_http`，配置签名 `matched`。
   - native health：`http://127.0.0.1:30001/QueryDB/status` 在线，`IsLogin=1`。
8. 下一步残留：
   - 继续把 `topic_lifecycle` 接入更细的回复决策策略，例如对 `closed` 主题默认降权，对 `reopened` 主题优先使用最新追问。
   - 群聊聚合继续增强 mention、sender relevance、近端上下文相关度，减少多群友同时发言时的机械合并。
   - sidebar 可继续做更接近真实点击的浏览器级交互巡检，覆盖每个按钮的 loading、错误态和状态刷新。
   - 需要用户配合时，优先做多私聊并行、群聊 owner 发言、新消息后连续 tick 不重复、普通文件收发后的 ledger/memory 闭环实机复测。

## 2026-07-10 01:18 长跑 worker 终态化与总台收敛

1. 本轮目标：
   - 按 `后续完整计划书.md` 的推荐入口，推进“长跑并发和 worker 终态化”。
   - 优先处理阶段 A/P0：Agent worker、WeFlow worker、send bridge worker 停止、崩溃或心跳过期后，任务总台不能继续留下假 running/active。
2. 当前状态巡检：
   - task counts：blocked=0，queued=0，running=0，waiting=0，active=0，total=95。
   - confirm counts：pending=0，approved=0，queued_to_bridge=0，accepted=0，sent=0，failed=3，rejected=0。
   - bridge pending=0。
   - send bridge worker：external_process，pid=2608，pid_alive=true，config_status=matched，backend=wechat_native_http，heartbeat_age_seconds≈0.19。
   - native health：`send-driver-probe` 通过，`http://127.0.0.1:30001` 在线，`IsLogin=1`。
3. 本轮修改：
   - `app/personal_wechat_bot/tasks/manager.py`
     - 新增显式 worker 任务的 liveness reconciliation。
     - 对 `metadata.worker=true` 或明确 worker scope/external_id 的 running 任务，检查 `worker_pid` 和 `worker_heartbeat_at`。
     - PID 已不存在时终态化；heartbeat 超过 `worker_stale_after_seconds` 时标记 stale failed。
     - fresh heartbeat 的 worker 不会被误终态化，普通业务 running 任务不受影响。
   - `app/personal_wechat_bot/control/sidebar_api.py`
     - `build_sidebar_task_manager()` 前的 runtime reconciliation 从只处理 Agent 扩展为同时处理 WeFlow worker 和 send bridge worker。
     - Agent/WeFlow worker task 写入 `worker_pid`、`worker_heartbeat_at`、`worker_stale_after_seconds`、`worker_kind` 等结构化元数据。
   - `tests/test_task_manager.py`
     - 新增 stale heartbeat worker 终态化、fresh worker 保持 running、WeFlow worker 停止收敛、send bridge worker 停止收敛测试。
4. 不变量影响：
   - 未修改真实发送链路、bridge outbox/ack、确认队列状态机或 native hook。
   - 不触碰 `data/send_bridge` 证据文件。
   - 仅对显式 worker 任务做终态化，避免误杀普通对话/发送业务任务。
5. 验证：
   - 聚焦测试：5 tests OK。
   - `python -m unittest tests.test_send_bridge_worker tests.test_send_commands tests.test_task_manager tests.test_sidebar_api -v`：220 tests OK。
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`：通过。
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK，`pending_count=0`，worker `matched`。
   - `git diff --check -- app\personal_wechat_bot\tasks\manager.py app\personal_wechat_bot\control\sidebar_api.py tests\test_task_manager.py`：无 whitespace error，仅既有 CRLF warning。
6. 真实数据影响：
   - 本轮读取了真实 `data` 状态，并触发任务总台的安全修复读路径。
   - 未清理历史。
   - 未删除或改写 `data/send_bridge` 的 outbox/ack/synced/reverify/lock 证据。
7. 下一步残留：
   - 继续压测 Agent worker、WeFlow worker、send bridge worker 同时运行时的任务生命周期，尤其是 worker 崩溃后 supervisor give-up 与任务失败状态的同步。
   - 给前端总台增加更清楚的 worker 状态解释：sidebar-managed、external_process、stale config、lock held、worker down/backlog。
   - 继续推进通道身份隔断和长对话聚合质量。
   - 继续完善 agent 自发文件在 ledger、memory summary、entities 中的闭环。

## 2026-07-10 00:48 旧 approved sidebar 探针收敛

1. 本轮发现的问题：
   - 真实 `data` 中还残留一个旧测试项：`sidebar_channel_test_reply:9721c38b6126`。
   - 该项是 sidebar 手动通道文本探针，`send_metadata.channel_test=true`，但在此前 stale worker 修复后任务 `detail` 变成了 `obsolete_bridge_worker_stale_config:current_backend=wechat_native_http`，不再命中旧的 `detail == confirm_approved` 退休规则。
   - 结果是总台显示 active=1/queued=1，但 `send_bridge.pending_count=0`，看起来像“总台卡住”，实际是旧 approved 探针没有退场。
2. 本轮修复：
   - `TaskStatusStore` 的旧 sidebar 探针退休规则前置：在 `mode=auto` 且 `send_confirm_required=false` 时，只要 `message_id` 以 `sidebar_channel_test_` 开头、任务仍是 `queued`、且没有 `external_id/bridge_id`，就标记为 `obsolete_sidebar_confirm_test_task:auto_mode_active`。
   - 普通 approved 回复不受影响；已有回归确认普通 stale worker approved 回复仍会重新打开等待重投。
   - `list_confirm_queue()` 继续负责把已标记 obsolete 的 approved sidebar 探针从 `approved` 转为 `failed`，并写入发送审计。
3. 真实数据收敛结果：
   - 总台：active=0，queued=0。
   - 确认队列：approved=0。
   - `sidebar_channel_test_reply:9721c38b6126` 已进入 failed，reason=`obsolete_sidebar_confirm_test_task:auto_mode_active`。
   - `send_bridge.pending_count=0`，native HTTP 在线，worker 配置仍为 matched。
4. 新增回归：
   - `test_auto_mode_retires_sidebar_test_reopened_after_stale_worker_repair`
   - `test_list_confirm_queue_retires_reopened_sidebar_test_after_stale_worker_repair`
5. 本轮验证：
   - 聚焦测试：5 tests OK。
   - `python -m unittest tests.test_send_bridge_worker tests.test_send_commands tests.test_task_manager tests.test_sidebar_api -v`：216 tests OK。
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`：通过。
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK，`pending_count=0`，worker `matched`。
   - `git diff --check -- app\personal_wechat_bot\tasks\manager.py tests\test_task_manager.py tests\test_send_commands.py 计划书.md`：无 whitespace error，仅既有 CRLF warning。
6. 下一步残留：
   - 继续长跑并发压测：Agent worker、WeFlow worker、send bridge worker、审核队列同时运行时的任务状态收敛。
   - 继续检查 worker 崩溃/重启后 running 任务终态化，避免旧 worker 任务再次卡住总台。
   - 继续推进通道身份隔断和长对话聚合质量。
   - 继续完善 agent 自发文件在 ledger、memory summary、entities 中的闭环。

## 2026-07-10 00:40 任务总台 SQLite 残留净化与并发写回加固

1. 本轮发现并修复的问题：
   - UI 探针/本地诊断任务虽然已从总台视图中过滤，但如果残留已经进入 `scheduler.sqlite` 权威库，旧读路径可能只返回过滤后的视图，不把净化结果写回 SQLite 和 `task_manager/tasks.json` 投影。
   - 这会留下“前端看不见、库里还在”的死条目，长跑后容易干扰任务状态审计和历史清理判断。
2. 本轮修复：
   - `TaskStatusStore._read_tasks()` 现在只要发现过滤结果或状态修复结果与 SQLite 当前任务列表不一致，就会触发权威库净化。
   - 净化写回改为走 `SchedulerStore.update_tasks_atomically()`，在 SQLite 写锁内重新读取最新任务、过滤 UI 探针、修复 bridge/ledger/后端阻断状态，避免读到旧列表后覆盖其它线程刚写入的任务。
   - JSON 投影写回改为净化后重新从 SQLite 读取，减少并发时投影落后或覆盖新任务的风险。
   - 本轮未修改 `data/send_bridge`，也未清理 outbox/ack/worker 状态。
3. 新增回归：
   - `test_ephemeral_ui_tasks_are_purged_from_sqlite_on_read`：直接向 `scheduler.sqlite` 注入一个 UI 探针残留和一个真实任务，读取状态后确认视图、SQLite、JSON 投影均只保留真实任务。
4. 本轮验证：
   - `python -m unittest tests.test_task_manager.TaskStatusStoreTest.test_ephemeral_ui_tasks_are_purged_from_sqlite_on_read tests.test_task_manager.TaskStatusStoreTest.test_ephemeral_ui_diagnostics_are_hidden_from_persistent_state tests.test_task_manager.TaskStatusStoreTest.test_local_ui_conversation_probe_tasks_are_hidden_from_persistent_state -v`：3 tests OK。
   - `python -m unittest tests.test_send_bridge_worker tests.test_send_commands tests.test_task_manager tests.test_sidebar_api -v`：214 tests OK。
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK，native HTTP 在线，bridge worker 配置 matched，`pending_count=0`。
   - 真实 `data` 总台读取：active=1，唯一 queued 项为 `send-sidebar_channel_test_reply9721c38b6126`；确认队列 `approved=1`，对应 `sidebar_channel_test_reply:9721c38b6126`，不是 bridge pending 堆积。
   - `git diff --check -- app\personal_wechat_bot\tasks\manager.py tests\test_task_manager.py 计划书.md`：无 whitespace error，仅既有 CRLF warning。
5. 下一步残留：
   - 对 `sidebar_channel_test_reply:9721c38b6126` 这类旧 approved 测试项提供明确处理：重投、移除或继续保留，避免总台长期 active=1 但后台无 bridge pending。
   - 继续检查长跑时 Agent worker、WeFlow worker、send bridge worker 同时运行后的任务状态收敛，尤其是 worker 崩溃/重启后的 running 任务终态化。
   - 继续检查历史清理边界，确保 `data/send_bridge` 已跑通证据与必要运行态始终被保留。
   - 继续压测通道隔断和身份边界：账号本人手动消息、agent 自发消息、用户消息、多私聊/多群聊。
   - 继续推进长对话聚合质量和 agent 自发文件在 ledger/memory/entities 中的闭环。

## 2026-07-10 00:32 真实发送链路验收确认与残留清理入口

1. 本轮实机验收结论：
   - 不选 talkers 时直接阻断，通道白名单/无 talker 发送保护生效。
   - 自动发送成功，存在一定延迟，但属于 bridge 投递、native 发送、readback/ack 状态收敛的合理窗口。
   - 普通文件也可以自动投递，前端已显示已发送，微信侧已看到真实文件消息。
   - 当前对话机制已基本完善，后续重点从“打通链路”切换到“加固并发、清理历史、隔断通道、收敛任务状态”。
2. 本轮本机复核：
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK。
   - `bridge_outbox` 已注册，`real_send_implemented=true`，`send_backend=wechat_native_http`。
   - native HTTP 在线：`http://127.0.0.1:30001`，`IsLogin=1`。
   - send bridge worker 运行中，配置 `matched`，`pending_count=0`，当前没有待投递堆积。
3. 本轮回归验证：
   - `python -m unittest tests.test_send_bridge_worker tests.test_send_commands tests.test_task_manager tests.test_sidebar_api -v`：213 tests OK。
   - `git diff --check` 覆盖本轮相关后端、任务、审核和前端状态文件：无 whitespace error，仅既有 CRLF warning。
4. 后续计划残留：
   - 并发加固：连续 Agent、WeFlow 拉取、send bridge worker、审核队列、任务管理同时运行时，继续检查 running/queued/waiting/stale 是否稳定收敛。
   - 历史清理：继续明确“可清理历史”和“必须保留证据/运行态”的边界，尤其不能误删已跑通的 `data/send_bridge` outbox/ack/worker 状态。
   - 通道隔断：继续压测账号本人手动消息、agent 自发消息、用户消息、多私聊/多群聊之间的身份边界。
   - 总台调度：继续检查任务与资源底部条目是否能随 bridge ack、Agent tick、后台拉取正确完成和移除。
   - 长对话聚合：继续推进底部语境优先、减少追答旧历史、增强重复惩罚、逐通道独立聚合。
   - 文件适配：继续完善 agent 自发文件在通道文件、conversation ledger、memory summary/entities 中的记录与回读。

## 2026-07-10 00:14 历史清理并发保护

1. 本轮发现并修复的逻辑问题：
   - `clear_sidebar_history_data()` 的清理名单包含 `runtime/agent_state.json`，但旧的保留逻辑把整个 `runtime/` 目录作为配置目录保留，导致旧 Agent cursor/tick 状态实际不会被清掉。
   - 已改为只保留明确配置文件，不再用 `runtime/` 父目录挡住历史运行态清理。
   - 新回归确认：清历史会删除 `runtime/agent_state.json`，但保留 `runtime/sidebar_launch.json` 和 `runtime/resource_audit.json`。
2. 本轮并发加固：
   - 直接调用历史清理 API 时，如果当前进程内有连续 Agent、WeFlow 后台拉取、WeFlow pull-once 或 backfill worker 正在运行，会返回 `blocked/history_clear_runtime_active`。
   - 前端正常清历史仍走 `shutdown_processes`，由 helper 停 sidebar/WeFlow 后再清理，不受阻断影响。
   - `shutdown_helper` 和显式 `force` 可绕过该保护，用于已完成停机后的清理流程。
3. 已保住的发送链路：
   - `data/send_bridge/outbox.jsonl`
   - `data/send_bridge/acks.jsonl`
   - `data/send_bridge/synced_acks.json`
   - `data/send_bridge/accepted_reverify.json`
   - `data/send_bridge/.bridge_worker.lock`
4. 本轮验证：
   - `python -m unittest tests.test_sidebar_api tests.test_sidebar_history_reset_shutdown tests.test_task_manager tests.test_send_commands tests.test_send_bridge_worker -v`：212 tests OK。
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK，native HTTP 在线，bridge worker 配置 matched，`pending_count=0`。
   - `git diff --check -- app\personal_wechat_bot\control\sidebar_api.py tests\test_sidebar_api.py`：无 whitespace error，仅既有 CRLF warning。
5. 下一步残留：
   - 继续检查总台任务状态收敛：长跑后 running/queued/waiting 是否有旧任务僵死。
   - 继续压测通道隔断：账号手动消息、agent 自发消息、用户消息、多私聊/多群聊之间的身份边界。
   - 继续做长对话聚合质量：底部语境优先、减少旧历史追答、增强重复惩罚。
   - 继续补 agent 自发文件在 ledger、通道文件、记忆摘要/实体中的适配。

## 2026-07-10 00:05 实机验收收束与后续加固入口

1. 本轮实机验收结论：
   - 不选 talkers 时可以直接阻断，发送保护生效。
   - 自动发送链路成功，文本有一定延迟，但属于 bridge accepted/readback verified 的正常收敛窗口。
   - 普通文件也可以自动投递，前端显示已发送，微信侧已经看到真实文件消息。
   - 当前对话机制基本完善，后续主要进入加固、清理计划残留、base 优化和 bug 扫描阶段。
2. 当前本机探针状态：
   - `send-driver-probe` 通过。
   - native HTTP 在线，`http://127.0.0.1:30001` 可用，`IsLogin=1`。
   - `bridge_outbox` 已注册且 `real_send_implemented=true`。
   - send bridge worker 运行中，backend=`wechat_native_http`，配置 `matched`，`pending_count=0`。
3. 已保住的关键通路：
   - 微信真实非前台文本输出。
   - 普通文件真实非前台输出，并通过 WeFlow/readback 逐步升级到 `sent/verified`。
   - 无 talker 保护、通道白名单授权、自动审核推进、bridge outbox/ack 回写。
   - scoped cursor 和连续 Agent worker scope 保护，避免选定 talker 时误消费其它通道事件，避免运行中悄悄切换作用域。
4. 后续长任务残留入口：
   - 并发加固：连续 Agent、WeFlow 拉取、send bridge worker、审核队列、任务管理同时运行时，确认任务状态稳定收敛，不出现 stale/running/queued 僵死。
   - 历史清理：设计明确的清理边界，保留已跑通证据与必要运行态，不误删 `data/send_bridge` 中的 outbox/ack/worker 状态。
   - 通道隔断：继续压测多私聊、多群聊、账号手动消息、agent 自发消息、用户消息之间的身份隔离。
   - 总台调度：检查任务与资源列表是否能随 bridge ack、Agent tick、后台拉取正确完成和移除。
   - 长对话聚合：底部语境优先，减少追答旧历史，增强重复惩罚，保证逐通道独立聚合而不是跨通道混合。
   - 文件适配：继续完善 agent 自己发出的文件在通道、对话 ledger、记忆摘要和实体文件中的记录与回读。
   - 图片/GIF 发送仍保持 unsupported，除非后续明确要做表情包或图片活人感。

## 2026-07-09 23:56 通道隔离与 scoped cursor 加固

1. 本轮实机反馈已确认：
   - 不选 talkers 时前端发送/Agent 启动阻断正常。
   - 自动文本发送、普通文件自动投递均已真实成功，微信侧可见。
   - 阻断链路可生效，当前对话机制进入加固阶段。
2. 本轮新增加固：
   - `BackendEventJsonlDriver` 支持按 `conversation_id` 过滤读取结果，Agent 选定 talker 后不再消费其它通道的新事件。
   - Agent 游标改为按作用域隔离：同一 `backend_events.jsonl` 下，不同 talker/conversation scope 使用独立 cursor，避免“选 A 时跳过 B，之后 B 丢消息”。
   - 后端 `_agent_requested_conversation_ids()` 增加 `chat_title`、`sender_names` 别名匹配；前端 `conversationIdsForTalkers()` 同步支持展示名和 sender_names。
   - 新增回归测试：scoped Agent 不吞其它 talker；随后切到其它 talker 仍能处理；重复跑同一 scope 不重复处理；展示名/发送者名可解析到已登记通道。
3. 本轮验证：
   - `python -m unittest tests.test_sidebar_api.SidebarApiTest.test_sidebar_agent_tick_scoped_cursor_does_not_consume_other_talkers tests.test_sidebar_api.SidebarApiTest.test_agent_requested_conversation_ids_accepts_channel_display_aliases tests.test_sidebar_api.SidebarApiTest.test_sidebar_agent_tick_without_scope_keeps_global_pending_scan_after_events tests.test_sidebar_api.SidebarApiTest.test_sidebar_agent_tick_reads_session_and_runs_reply_closed_loop -v`
   - `python -m unittest tests.test_sidebar_api tests.test_backend_events tests.test_conversation_ledger tests.test_channel_state_store tests.test_send_commands tests.test_task_manager -v`：238 tests OK。
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js` 通过。
   - `git diff --check` 无 whitespace error；仅保留既有 CRLF warning。
4. 后续残留：
   - 继续压测连续 Agent + WeFlow 拉取 + send_bridge worker 并发时的任务状态收敛。
   - 清理历史时继续确认 `send_bridge/outbox.jsonl`、`acks.jsonl`、`synced_acks.json`、`accepted_reverify.json`、`.bridge_worker.lock` 不被误删。
   - 继续提升长对话聚合质量：底部语境优先、跨通道隔断、少追答历史、重复惩罚更强。

## 2026-07-09 22:30 最新状态

1. 实机验收已确认：
   - 不选 talkers 时发送会被阻断。
   - 自动文本发送成功；延迟属于 accepted/unverified 到 sent/verified 的正常收敛窗口。
   - 普通文件可以自动投递，前端显示已发送，微信侧已看到真实消息。
   - 阻断链路可生效。
2. 本轮加固：
   - 保留 native HTTP + bridge_outbox 工作链路，不清理 `data/send_bridge` 历史证据。
   - `memory/summary.md` 现在会写入 `Recent Files`，包含 agent/owner/user 文件的 origin、direction、send_status、bridge_id、external_message_id 等关键字段。
   - file-only 的 agent 回复也会进入记忆摘要，不再因为没有正文而消失。
   - LLM 记忆覆盖时会把确定性的文件实体合并回 `entities.json`，避免模型摘要吞掉真实文件回执字段。
   - 文件读回验证改为“唯一匹配才提升为 sent”：同一通道短时间出现多条同名同大小 outgoing 文件时保持 accepted/unverified，避免并发误验。
3. 当前探针：
   - `send-driver-probe` 通过。
   - native HTTP 在线，`IsLogin=1`。
   - send bridge worker 配置匹配，backend=`wechat_native_http`，pending=0。
   - 文件能力为 `default_route_accepts_unverified_native_file`，符合“先 accepted，读回后 sent”的当前设计。


## 2026-07-09 本轮状态

### 2026-07-09 15:40 更新

1. 已确认用户侧观测到真实文本消息发送；本机复验也通过：
   - 新 native DLL 部署后，`/QueryDB/status` 返回 `IsLogin=1`。
   - outbox 文本回归 `bridge:native-hook-regression:920f9b15c83e` 达到 `sent / wechat_native_http_send_text_verified`。
   - WeFlow 回读命中 outgoing `serverId=7956662134991012669`。

2. 已新增普通文件发送 native 诊断端口：
   - 新增 `/debug/file-send/status`
   - 新增 `/debug/file-send/enable`
   - 新增 `/debug/file-send/disable`
   - 新增 `/debug/file-send/clear`
   - 新增 `/debug/file-send/events`
   - 默认关闭；当前已在运行中的微信进程启用。

3. 诊断 hook 当前覆盖 4 个候选函数：
   - upload init: `0x52302c0`
   - upload short wrapper: `0x5236820`
   - sendfile init: `0x527cf70`
   - sendfile short wrapper: `0x5281990`

4. 已修复诊断 hook 的返回值风险：
   - short wrapper 返回 `int`。
   - init 长函数返回 64 位对象/指针，已使用独立 `void*` 签名，避免截断返回值。

5. 已部署到微信目录：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-153920`

### 文件诊断待采样

1. 需要一次手动普通文件发送样本：
   - 在微信中任选一个在库私聊或文件传输助手。
   - 手动发送一个很小的 `.txt` 或 `.csv` 普通文件。
   - 不需要用 agent/前端发；这里是为了捕获微信自身的真实 native 参数。

2. 采样后读取：
   - `GET http://127.0.0.1:30001/debug/file-send/events`
   - 重点看事件里的接收人、文件路径、文件名、upload ticket/app attach id 等字符串候选。

3. 只有拿到样本并确认参数结构后，才允许把 `/send_file_msg` 从 hard unsupported 改成真实投递。

### 2026-07-09 15:47 更新

1. 发现并修复诊断事件读取问题：
   - 诊断状态曾显示 `event_count=2`，但 `/debug/file-send/events` 返回 HTTP 500。
   - 原因是微信内部结构里的候选字符串可能不是合法 UTF-8，`json.dump()` 严格序列化时抛异常。
   - 已改为候选字符串先做 UTF-8 校验；合法时写入 `value`，不合法时只写入 `value_hex` 和 `ascii_preview`。
   - `SetJson()` 同时使用 JSON replace error handler 做最后兜底。

2. 新增采样脚本：
   - `scripts/capture_wechat_file_send_diagnostics.py`
   - 可执行：`python scripts\capture_wechat_file_send_diagnostics.py --clear --enable --wait 60`
   - 期间手动发送一个普通小文件后，脚本会保存完整事件到 `data/native_diagnostics/`。

3. 已重新构建并部署：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-154453`
   - 当前 DLL 长度: `525312`

4. 部署后验证：
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/enable`: 4 个 hook 全部启用成功。
   - `/debug/file-send/events`: 正常返回 JSON，不再 500。
   - 文本回归 `bridge:native-hook-regression:cf80e7bc5f3a` 达到 `sent / wechat_native_http_send_text_verified`。
   - 文本回归没有误触文件 hook，`event_count=0`。

### 2026-07-09 15:51 更新

1. 为文件发送 native 诊断增加持久落盘：
   - 捕获事件时除了写入内存 ring buffer，还会追加 JSONL 到 `%TEMP%\wechat_native_file_send_diagnostics.jsonl`。
   - `/debug/file-send/status` 现在返回 `persistent_log_path`。
   - `/debug/file-send/clear` 会同时清空内存事件和持久日志。
   - 目的：避免手动样本只存在微信进程内存里，接口异常或微信重启后丢失。

2. 更新采样脚本：
   - `scripts/capture_wechat_file_send_diagnostics.py` 会读取 status 中的 `persistent_log_path`。
   - 如果存在持久 JSONL，会复制到 `data/native_diagnostics/` 同名样本旁边。

3. 已重新构建并部署：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-155018`
   - 当前 DLL 长度: `529920`

4. 部署后验证：
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/status`: 4 个 hook 全部启用，且返回持久日志路径。
   - 采样脚本 `py_compile` 通过，并可正常保存空事件样本。
   - 文本回归 `bridge:native-hook-regression:76297d5653e5` 达到 `sent / wechat_native_http_send_text_verified`。
   - 文本回归未误触文件 hook；内存事件和持久日志均为空。

### 2026-07-09 15:56 更新

1. 增强文件发送 native 诊断的字符串识别：
   - 继续保留微信字符串结构候选扫描。
   - 新增 UTF-16/宽字符串微信结构扫描，用于捕获文件路径、文件名等 Windows/微信常见宽字符串字段。
   - 新增原始 byte string 扫描。
   - 新增原始 UTF-16LE 扫描。
   - 扫描仍限制在参数指针附近的小窗口，只读，不修改微信内部参数。

2. 增强采样脚本：
   - summary 现在会列出 `string_candidates` 和 `raw_string_candidates`。
   - summary 会保留 `encoding`、`value`、`ascii_preview`、`value_hex`，方便从样本中快速定位 wxid、文件路径、文件名、ticket/app attach id。
   - 如果 HTTP events 端点异常，脚本会回退读取 native 持久 JSONL。

3. 已重新构建并部署：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-155512`
   - 当前 DLL 长度: `539136`

4. 部署后验证：
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/events`: 正常返回 JSON。
   - 采样脚本可正常保存空事件样本。
   - 文本回归 `bridge:native-hook-regression:6830c8ce2f80` 达到 `sent / wechat_native_http_send_text_verified`。
   - 文本回归未误触文件 hook，持久日志为空。
   - `/send_file_msg` 仍返回 `unsupported_on_411053_file_upload_not_mapped / accepted=false`，没有伪报文件发送成功。

### 2026-07-09 16:01 更新

1. 复核文件诊断 hook ABI：
   - short wrapper 和 init long 函数均只使用前 4 个寄存器参数和第 5 个栈参数。
   - 当前 5 参数 detour 签名不会丢失第 6/第 7 参数。
   - init long 函数继续保持 64 位指针返回签名。

2. 增强 native 诊断事件：
   - 新增 `return_address` 和 `return_weixin_offset`，用于定位 upload/sendfile wrapper 的真实调用点。
   - 每个参数对象新增一级 `pointer_fields` 浅层扫描。
   - 对一级指针字段记录偏移、目标指针、目标 preview hex、目标字符串候选。
   - 仍然只读、小窗口、不递归，避免对微信线程造成明显扰动。

3. 已重新构建并部署：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-160027`
   - 当前 DLL 长度: `543232`

4. 部署后验证：
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/enable`: 4 个 hook 全部启用。
   - `/debug/file-send/events`: 正常返回 JSON。
   - 采样脚本可保存空事件样本。
   - 文本回归 `bridge:native-hook-regression:945b45f05ab4` 达到 `sent / wechat_native_http_send_text_verified`。
   - 文本回归未误触文件 hook，持久日志为空。
   - `/send_file_msg` 仍返回 `unsupported_on_411053_file_upload_not_mapped / accepted=false`。

### 2026-07-09 16:06 更新

1. 修正文件诊断调用点记录：
   - 发现上一版 `_ReturnAddress()` 在 `CaptureEvent()` 辅助函数内读取，实际会指向 detour 内部返回点，而不是微信原始调用点。
   - 已改为在每个 detour 入口层读取 `_ReturnAddress()`，再传入 `CaptureEvent()`。
   - 后续样本中的 `return_weixin_offset` 才能用于定位真实 upload/sendfile 调用点。

2. 增强采样脚本摘要：
   - summary 现在包含 `return_address`、`return_weixin_offset`。
   - summary 会提取一级 `pointer_fields` 里的字符串候选，便于快速定位 wxid、路径、文件名、attach id。

3. 已重新构建并部署：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-160454`
   - 当前 DLL 长度: `543232`

4. 部署后验证：
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/enable`: 4 个 hook 全部启用。
   - `/debug/file-send/events`: 正常返回 JSON。
   - 采样脚本可保存空事件样本。
   - 文本回归 `bridge:native-hook-regression:2aa36c73e1d2` 达到 `sent / wechat_native_http_send_text_verified`。
   - 文本回归未误触文件 hook，持久日志为空。
   - `/send_file_msg` 仍返回 `unsupported_on_411053_file_upload_not_mapped / accepted=false`。

### 2026-07-09 16:10 更新

1. 增强文件诊断采样脚本的自动线索提取：
   - 新增顶层 `hints`。
   - 自动分类候选值：`receiver_wxid`、`receiver_roomid`、`file_path`、`file_path_fragment`、`file_name`、`upload_endpoint`、`sendfile_endpoint`、`appattach_field`、`id_or_token` 等。
   - hints 会保留 wrapper、return_weixin_offset、arg、source、offset、pointer_offset、encoding、value/value_hex。
   - 采样后无需先人工翻完整大 JSON，可以先看 `hints` 高分项。

2. 增加轻量测试：
   - 新增 `tests/test_capture_wechat_file_send_diagnostics.py`。
   - 覆盖 wxid、Windows 文件路径、文件名、upload endpoint 的自动分类。

3. 本轮未改 native DLL，未重启微信。

4. 验证：
   - `python -m py_compile scripts\capture_wechat_file_send_diagnostics.py`
   - `python -m unittest tests.test_capture_wechat_file_send_diagnostics`
   - `python scripts\capture_wechat_file_send_diagnostics.py --enable --wait 0 --min-events 0`
   - `/debug/file-send/status`: 4 个 hook 仍在线。
   - `/api/state`: `bridge_outbox_ready`，worker `matched`，active task=0。

### 2026-07-09 16:14 更新

1. 增强采样脚本为“采样 + 离线分析”双模式：
   - `python scripts\capture_wechat_file_send_diagnostics.py --input <sample.json>`
   - `python scripts\capture_wechat_file_send_diagnostics.py --input <native-log.jsonl>`
   - 默认输出 `<原文件名>.analyzed.json`，不覆盖原始样本。
   - 离线输入即使事件数为 0 也返回成功，便于分析历史样本。

2. `hints` 继续作为顶层线索汇总：
   - 实时采样和离线分析都会重新生成 `summary` 与 `hints`。
   - 可用于快速定位 `receiver_wxid/roomid`、`file_path`、`file_name`、`upload_endpoint`、`sendfile_endpoint`、`appattach_field`、`id_or_token`。

3. 扩展测试：
   - `tests/test_capture_wechat_file_send_diagnostics.py` 增加 JSONL 输入解析与默认 analyzed 输出路径测试。

4. 本轮未改 native DLL，未重启微信。

5. 验证：
   - `python -m py_compile scripts\capture_wechat_file_send_diagnostics.py`
   - `python -m unittest tests.test_capture_wechat_file_send_diagnostics`
   - `python scripts\capture_wechat_file_send_diagnostics.py --input data\native_diagnostics\<latest>.json`
   - `/debug/file-send/status`: 4 个 hook 仍在线。
   - `/api/state`: `bridge_outbox_ready`，worker `matched`，active task=0。

### 已完成

1. 文本真实发送链路已打通并验收：
   - native hook `/SendTextMsg` 可真实投递到当前微信。
   - bridge outbox -> send bridge worker -> native HTTP -> WeFlow 回读验证完整通过。
   - 实测 ack: `sent / wechat_native_http_send_text_verified`，并拿到 WeFlow outgoing `serverId`。

2. 修复旧 worker 配置残留：
   - 发现旧 `.bridge_worker.lock` 仍是 `wechat_hook_http`，导致前端 stale/accepted 状态污染。
   - 重启 sidebar 后启动独立 `send_bridge_worker.py`，当前 lock 为 `wechat_native_http` 且 `config_status=matched`。
   - `/api/state` 当前 `background_send_status=bridge_outbox_ready`。

3. 增强启动与 readiness：
   - `scripts/start_sidebar_frontend.py` 启动时会尝试确保 send bridge worker 存在。
   - readiness 的 native 文本验证说明改为支持 hook 自证或 WeFlow 回读。
   - worker 活锁检查会比较当前配置签名，避免旧 backend lock 被误判健康。

4. 清理任务残留：
   - 本地 UI 探针任务不再污染持久任务视图。
   - 旧 `weflow_text_send_not_supported:native-not-implemented` blocker 在当前 backend 已切到 `wechat_native_http` 后会被标记为 obsolete failed。
   - 当前真实数据任务统计：active=0。

5. WCF 链路清理核验：
   - 当前源码扫描未发现 `wcf/wcferry` 残留接口。
   - 前端配置输出已是 `wechat_native_*` 字段。

### 验证

1. `python -m unittest tests.test_send_readiness tests.test_sidebar_api`
2. `python -m unittest tests.test_send_backends tests.test_send_bridge_worker tests.test_bridge_send tests.test_send_commands`
3. `python -m unittest tests.test_task_manager`
4. `node --check app/personal_wechat_bot/ui/sidebar/app.js`
5. 实机 outbox 文本探针：
   - bridge id: `bridge:native-hook-outbox-probe:70ba3cc811dc`
   - ack: `sent`
   - reason: `wechat_native_http_send_text_verified`
6. 诊断版 native hook 构建：
   - `powershell -ExecutionPolicy Bypass -File .\scripts\build_wechat_native_hook.ps1`
   - 结果：0 warnings / 0 errors。
7. 诊断版部署后文本回归：
   - bridge id: `bridge:native-hook-regression:920f9b15c83e`
   - ack: `sent`
   - reason: `wechat_native_http_send_text_verified`
   - external message id: `7956662134991012669`
8. 文件端口防误报验证：
   - `POST /send_file_msg`
   - 结果：`ret=2 / unsupported_on_411053_file_upload_not_mapped / accepted=false`
9. 回归测试：
   - `python -m unittest tests.test_send_backends tests.test_send_bridge_worker tests.test_send_readiness tests.test_sidebar_api`
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`

## 仍然残留

1. 普通文件真实发送尚未完成：
   - 当前 `/send_file_msg` 仍保持明确 unsupported。
   - 不能把文件投递伪装成 accepted/sent。
   - 已确认 4.1.10.53 文件发送是 `uploadappattach` + `sendfileuploadmsg` 两段 native 流程。

2. 文件 native 逆向进展：
   - upload wrapper: `0x5236820`，唯一直接调用点 `0x5236572`。
   - sendfile wrapper: `0x5281990`，唯一直接调用点 `0x52816e2`。
   - long/init 候选：`0x52302c0`、`0x527cf70`。
   - 下一步需要继续复原参数结构，或加临时 hook 记录手动文件发送时的真实参数。

3. agent 自己发出的文件通道、对话文件、记忆文件适配还未做。

4. 长对话聚合质量仍需继续增强：
   - 主题聚合、接话、主动打招呼已可并行推进。
   - 需要继续降低重复回复风险，强化“只回复当前聊天底部合理接话”的策略。

## 下一步

1. 继续 native 文件发送映射：
   - 优先普通文件，不做图片/表情。
   - 先通过 `/debug/file-send/events` 完成参数结构确认，再接 `/send_file_msg`。
   - 完成前继续保持文件发送 hard blocker。

2. 文件发送打通后：
   - 更新 bridge/backend ack 语义。
   - 扩展 WeFlow 回读或文件消息验证。
   - 适配 agent 文件输出、conversation ledger、memory summary/entities。

3. 再推进长对话：
   - 对每个通道独立聚合，不做跨通道混合。
   - 区分用户、账号本人手动发送、agent 发送。
   - 把“历史逐条回复”改成“基于当前会话底部状态接话”。

### 2026-07-09 16:28 更新

1. 文本真实发送补充验收：
   - 用户侧已观测到一条真实微信消息出现在前端聊天窗口。
   - 本轮复核 `/QueryDB/status`: `IsLogin=1`。
   - `/debug/file-send/status`: 文件诊断仍为 enabled，4 个 hook 均在线。
   - sidebar `/api/state`: `background_send_status=bridge_outbox_ready`，当前配置仍为 `wechat_native_http`。

2. 文件发送诊断采样：
   - 执行 `python scripts\capture_wechat_file_send_diagnostics.py --clear --enable --wait 90 --min-events 1`。
   - 输出 `data\native_diagnostics\file-send-events-20260709-082627.json` 与同名 `.report.md`。
   - 本次事件数为 0，说明该时间窗内未经过普通文件发送 native 路径；文本真实发送不会误触文件诊断 hook。

3. 新增只读反汇编辅助脚本：
   - 新增 `scripts/analyze_wechat_native_calls.py`。
   - 基于 `pefile + capstone` 读取本地 `Weixin-4.1.10.53.dll`，查找目标 RVA 的 direct `call rel32` xref 并输出上下文。
   - 已复核四个文件相关目标：
     - upload init `0x52302c0` 唯一 direct call: `0x522b2d1`
     - upload short `0x5236820` 唯一 direct call: `0x5236572`
     - sendfile init `0x527cf70` 唯一 direct call: `0x527bd21`
     - sendfile short `0x5281990` 唯一 direct call: `0x52816e2`

4. 静态判断更新：
   - upload/sendfile short wrapper 同构，均构造 endpoint 后再把第 5 个参数中的结构复制到请求对象。
   - 文件发送应继续复用当前文本发送的 UI 线程 dispatcher 模型，但不能走焦点、剪贴板、窗口模拟输入。
   - `/send_file_msg` 在参数结构确认前继续保持 hard blocker。

### 2026-07-09 16:36 更新

1. 文件诊断 native 侧增强：
   - `FileSendDiagnostics.cpp` 改为 before/after 双事件记录。
   - short wrapper 记录原函数返回 `int`。
   - init wrapper 记录原函数返回指针，并在返回值像指针时附带 `result_probe`。
   - 目标：手动文件样本到来后能同时看到调用前参数与调用后写回的 attach id/token 等线索。

2. 文件诊断报告增强：
   - `capture_wechat_file_send_diagnostics.py` 的 summary 增加 `phase/result_value/result_signed`。
   - Markdown 报告 Wrappers 表增加 `phases/results`，避免只在 JSON 深层才能看到返回值。

3. 新 native artifact：
   - 已执行 `powershell -ExecutionPolicy Bypass -File .\scripts\build_wechat_native_hook.ps1`。
   - 构建通过：0 warnings / 0 errors。
   - 新 artifact: `vendor\artifacts\wechat-native-411053\version.dll`，长度 `544256`。
   - 当前运行中的 `E:\aaa办公通讯\Weixin\version.dll` 仍为旧诊断版，长度 `543232`；尚未热替换，避免打断已验收的文本链路。

4. 新增安全部署脚本：
   - 新增 `scripts/deploy_wechat_native_hook.ps1`。
   - 默认从运行中的 `Weixin.exe` 推断微信根目录，避免脚本内中文路径被 Windows PowerShell 5 误解码。
   - 默认检测到微信运行中会拒绝覆盖，需关闭微信后执行；除非显式传 `-AllowRunning`。
   - 语法检查通过；当前运行中验证为按预期拒绝热替换。

5. 状态/审计复核：
   - send bridge worker 运行中，backend 为 `wechat_native_http`，配置签名 `matched`，pending/outbox 当前为 0。
   - 发送审计中的 `_compact` 报错是 2026-07-08 旧日志残留；当前 `ledger.py` 已定义 `_compact`。
   - 已验证 `python -m unittest tests.test_conversation_ledger tests.test_reply_gate`，43 tests OK。
   - 当前未观察到新的 ledger sync 或发送审计错误。

### 2026-07-09 16:45 更新

1. 已部署 before/after + deep fields 文件诊断版：
   - `FileSendDiagnostics.cpp` 新增 `schema=file_send_diagnostics_v2_before_after_deep_fields`。
   - 深层扫描覆盖常规近端偏移，以及 sendfile 静态反汇编暴露的 `+0x200/+0x328/+0x330/+0x350/+0x368/+0x420/+0x430/+0x438/+0x468/+0x500/+0x528/+0x540` 等关键偏移。
   - 记录 `string_candidates/raw_string_candidates/pointer_fields/numeric_fields`，用于定位 `filename/fileext/attachid/aeskey/fileuploadtoken/md5/totallen/uploadstatus` 等字段。

2. 部署与回归：
   - 先部署 before/after 版，备份：`E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-163908`，文本回归 `bridge:native-hook-regression:c6e56b153a0d` -> `sent / wechat_native_http_send_text_verified`，external id `7284995477676003365`。
   - 再部署 deep fields 版，备份：`E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-164300`。
   - 当前运行 DLL 与 artifact 均为 `546304` 字节。
   - `/debug/file-send/status` 当前显示 schema `file_send_diagnostics_v2_before_after_deep_fields`，4 个 hook 均在线。
   - deep fields 部署后文本回归 `bridge:native-hook-regression:fa2e726ad964` -> `sent / wechat_native_http_send_text_verified`，external id `7832523391479080552`。

3. 文件端口语义仍正确：
   - `POST /send_file_msg` 使用真实存在的本地 txt 文件测试。
   - 结果仍为 `ret=2 / unsupported_on_411053_file_upload_not_mapped / accepted=false / delivery_verified=false`。
   - 文本回归后 `/debug/file-send/status` 的 `event_count=0`，确认文本发送不会污染文件诊断事件。

4. 采样/报告验证：
   - `python scripts\capture_wechat_file_send_diagnostics.py --enable --wait 0 --min-events 0`
   - 输出 `data\native_diagnostics\file-send-events-20260709-084406.json` 与 `.report.md`。
   - 空样本可正常生成，报告包含新 schema。

5. 验证：
   - `python -m py_compile scripts\capture_wechat_file_send_diagnostics.py scripts\analyze_wechat_native_calls.py`
   - `python -m unittest tests.test_capture_wechat_file_send_diagnostics`
   - `powershell -ExecutionPolicy Bypass -File .\scripts\build_wechat_native_hook.ps1`
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`
   - `python -m unittest tests.test_send_backends tests.test_send_bridge_worker tests.test_send_readiness tests.test_sidebar_api`
   - `git diff --check`

6. 下一步仍然需要一个真实普通文件发送样本：
   - 现在诊断已经足够深；用户手动发送一个小 `.txt/.csv` 后，应能拿到 before/after 参数、返回值、深层字段和数值线索。
   - 拿到样本后再实现 `/send_file_msg`，不能在样本前把文件发送标成 accepted/sent。

### 2026-07-09 18:34 更新

1. 文本真实发送已作为当前基线确认：
   - 用户侧已观测到真实微信消息出现。
   - 本轮部署后两次 native 文本回归均成功，`/SendTextMsg` 返回 `accepted=true / dispatcher_ready=true / dispatched=true / native_result=1`。
   - `delivery_verified=false` 仍是当前 native HTTP 自身语义；只有 WeFlow 回读或其他读回验证确认后才会提升为 verified。

2. 文件发送诊断升级到 v8：
   - schema: `file_send_diagnostics_v8_owner_request_chain`。
   - 新增 owner/request 链路 hook：
     - owner factory `0x357b630`
     - owner ctor `0x3577220`
     - request ctor `0x366a880`
     - request derived ctor `0x366ecd0`
   - 保留并继续启用 submit/task/high/parent/upload/sendfile 既有 hook。

3. 已构建、部署、回归：
   - artifact: `vendor\artifacts\wechat-native-411053\version.dll`
   - 当前部署: `E:\aaa办公通讯\Weixin\version.dll`
   - 当前 DLL 长度: `552960`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-182907`
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/enable`: v8 所有 hook 均为 true。

4. 文件采样状态：
   - 执行 `python scripts\capture_wechat_file_send_diagnostics.py --clear --enable --wait 180 --min-events 16 --out data\native_diagnostics\file-send-events-v7-request-constructors-live.json`。
   - 本窗口未捕获到文件事件，输出事件数为 0；说明期间没有走普通文件 native 发送路径。
   - v8 诊断当前已在线，下一次手动发送普通文件时应能覆盖更完整的 owner -> request -> task -> upload/sendfile 链路。

5. 逻辑/残留扫描：
   - `git grep -i` 未在 `app/scripts/tests/vendor/reference/WeChat-Hook-aixed` 中发现 WCF/wcferry/WeChatFerry 残留命中。
   - `python -m unittest tests.test_capture_wechat_file_send_diagnostics` 通过。
   - `python -m unittest tests.test_send_backends tests.test_send_bridge_worker tests.test_reply_gate tests.test_sidebar_api` 通过，163 tests OK。
   - 测试输出中的 worker crash/timeout/locked traceback 是测试用例故意注入的故障场景，不是实机运行错误。

6. 当前文件端口策略不变：
   - `/send_file_msg` 继续保持 `unsupported_on_411053_file_upload_not_mapped / accepted=false`。
   - 在普通文件真实投递未验证前，不把文件状态伪装为 accepted/sent。

### 2026-07-09 18:46 更新

1. 文本真实发送链路继续作为稳定基线：
   - 用户侧已观测到真实微信消息出现。
   - 本轮两次部署 v9/native 诊断 DLL 后，均对 `filehelper` 做了 `/SendTextMsg` 回归。
   - 最新回归返回 `accepted=true / dispatcher_ready=true / dispatched=true / native_result=1 / dispatch_error=0`。
   - 文本回归未触发文件诊断事件，说明文本链路没有污染文件 hook 样本。

2. 文件发送诊断升级到 v9：
   - schema: `file_send_diagnostics_v9_upstream_path_chain`。
   - 新增上游只读 hook：
     - `sendfile_batch_entry`: `0x16af3e0`
     - `sendfile_request_builder`: `0x16aec20`
     - `sendfile_vector_submit`: `0x357ae50`
     - `sendfile_string_prepare`: `0x357bd20`
   - 保留 v8 的 request/submit/task/upload/sendfile 链路 hook。
   - `event_count` ring buffer 从 64 扩到 256，避免上游事件增多后挤掉关键后半链路。
   - 嵌套指针字段新增 raw byte / raw UTF-16 扫描，避免原始本地文件路径藏在普通 buffer 里却被漏掉。

3. 当前部署状态：
   - 当前运行 DLL: `E:\aaa办公通讯\Weixin\version.dll`
   - 当前 DLL 长度: `557568`
   - 最近备份: `E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-184541`
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/status`: v9 全部 hook 为 true。
   - 已清空诊断窗口，当前 `event_count=0`，等待下一次真实普通文件发送样本。

4. 额外发现：
   - 部署后曾短暂捕获 4 条事件，只包含 `sendfile_request_builder/request_ctor`，没有 `submit/task/upload/sendfile` 后半链路。
   - 这类事件不能作为“文件真实发送已打通”的证据，更像微信启动/内部恢复造成的半链路对象构造。
   - 已保存分析文件：`data\native_diagnostics\file-send-events-v9-current-4events.analyzed.json` 与同名 report。

5. 验证：
   - `python -m py_compile scripts\capture_wechat_file_send_diagnostics.py`
   - `python -m unittest tests.test_capture_wechat_file_send_diagnostics`
   - `powershell -ExecutionPolicy Bypass -File .\scripts\build_wechat_native_hook.ps1`
   - `python -m unittest tests.test_send_backends tests.test_send_bridge_worker tests.test_reply_gate tests.test_sidebar_api`
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`
   - `git diff --check` 仅输出既有 CRLF warning，无 whitespace error。

6. 下一步：
   - 需要在当前 v9 在线状态下手动向 `filehelper` 或一个在库私聊发送一个很小的普通 `.txt/.csv/.docx` 文件。
   - 发送后读取 `/debug/file-send/events`，重点确认是否同时出现 `sendfile_string_prepare/vector_submit/request_builder/submit_factory/task_factory/upload/sendfile`。
   - 如果 v9 能抓到原始本地路径和完整后半链路，再实现 `/send_file_msg`；否则继续上移 hook 到原始路径进入微信对象之前。

### 2026-07-09 19:20 更新

1. 文件发送 native 诊断升级到 v10：
   - schema: `file_send_diagnostics_v10_slim_upstream_chain`
   - 新增上游 hook：
     - `sendfile_vector_processor`: `0x16b0200`
     - `sendfile_context_dispatch`: `0x1669720`
   - 新增 file-item 构造链 hook：
     - `fileitem_default_ctor`: `0x6b5140`
     - `fileitem_base_ctor`: `0x6b5240`
     - `fileitem_business_ctor_a`: `0x18da5b0`
     - `fileitem_business_ctor_b`: `0x18df9f0`
     - `fileitem_shared_ctor`: `0x1768e40`
2. v10 诊断日志瘦身：
   - 不再把大块内存 preview 和无信号乱码全部写入 JSON。
   - 指针字段只在存在候选字符串时记录。
   - 新增 `std::vector<shared_ptr<fileitem>>` 快照，用于观察文件项 vector 中的对象字段。
3. 已重启微信、部署并启用 v10：
   - 部署文件：`E:\aaa办公通讯\Weixin\version.dll`
   - 当前 DLL 长度：`571392`
   - 最近备份：`E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-191604`
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/enable`: 全部 v10 hook 为 `true`
   - 当前 `event_count=0`
4. 验证：
   - `python -m py_compile scripts\capture_wechat_file_send_diagnostics.py`
   - `python -m unittest tests.test_capture_wechat_file_send_diagnostics`
   - `powershell -ExecutionPolicy Bypass -File .\scripts\build_wechat_native_hook.ps1`
   - `git diff --check -- vendor\reference\WeChat-Hook-aixed\src\FileSendDiagnostics.cpp scripts\capture_wechat_file_send_diagnostics.py tests\test_capture_wechat_file_send_diagnostics.py`
5. 当前阻塞点：
   - 用户提到的最新上传没有进入 v10 native 日志；工作区和 `%TEMP%` 也没有发现新的可用附件。
   - 需要在 v10 enabled 状态下再手动向 `filehelper` 或在库私聊发送一个很小的普通文件，才能确认本地路径进入 WeChat 对象的最早位置。
   - `/send_file_msg` 继续保持 hard unsupported，不能伪造文件 accepted/sent。
6. 版本管理注意：
   - `vendor/` 被 `.gitignore` 忽略，本轮 native C++ 修改不会出现在普通 `git status`。
   - 后续提交时需要强制纳入 `vendor/reference/WeChat-Hook-aixed/src/FileSendDiagnostics.cpp`，或调整忽略策略。

### 2026-07-09 19:26 更新

1. 修复 v10 初版诊断污染：
   - 文本回归时发现 `fileitem_default_ctor/base_ctor` 会被文本消息构造触发，导致文件诊断 `event_count=4`。
   - 已对 `fileitem_default_ctor` 跳过文本 shared_ptr 构造 callsite `0x64cb0d`。
   - 已对 `fileitem_base_ctor` 跳过文本默认构造 callsite `0x6b516f`。
   - 同时把字符串候选过滤收紧为 wxid/roomid、文件路径、文件名、appmsg XML、upload/sendfile 字段等强信号，避免再次生成几十 MB 级 JSON。
2. 已重新构建、重启微信并部署：
   - 部署文件：`E:\aaa办公通讯\Weixin\version.dll`
   - 当前 DLL 长度：`572416`
   - 最近备份：`E:\aaa办公通讯\Weixin\version.dll.codex-backup-20260709-192435`
   - `/QueryDB/status`: `IsLogin=1`
   - `/debug/file-send/enable`: v10 hooks 全部启用成功
3. 文本回归：
   - `POST /SendTextMsg` 到 `filehelper`
   - 返回：`accepted=true / dispatcher_ready=true / dispatched=true / native_result=1 / dispatch_error=0`
   - 回归后 `/debug/file-send/status event_count=0`，确认文本发送不再污染文件诊断。
4. 下一步不变：
   - 需要在当前 v10 enabled 状态下手动发送一个小普通文件，抓取真实 file-item/vector/context 事件。
   - 抓到新样本后再实现 `/send_file_msg` 真实文件投递入口。

### 2026-07-09 20:18 更新

1. 普通文件 native 投递已经从“未映射”推进到“端口接收 + WeChat 异步上传链路”：
   - `POST /send_file_msg` 到 `filehelper` 返回 `accepted=true / native_result=1 / dispatcher_ready=true / dispatched=true`。
   - native 诊断确认链路包含 `winapi_copy_file_w -> sendfile_submit_factory -> sendfile_task_factory -> sendfileuploadmsg_init/short -> cdninfo`。
   - 文件会被复制到 `E:\WeChat-doc\xwechat_files\wxid_p2ru43eyxtk322_adfb\msg\file\2026-07\...`，并触发 CDN 上传状态文件访问。
2. Python 桥接层已同步：
   - `wechat_native_file_send_blocker()` 不再阻断普通文件默认 `/send_file_msg`。
   - 图片/GIF 默认路由仍保持 unsupported，暂不做图片真实发送。
   - `wechat_native_send_capabilities().file.status` 改为 `default_route_accepts_unverified_native_file`，`supports=true`，`delivery_verified=false`。
   - `WeChatNativeHttpSendBackend.send_file()` live probe 返回 `wechat_native_http_send_file_accepted_unverified`。
3. 前端/说明已同步：
   - 非前台桥说明改为按在库通道解析出的 `wxid/roomid` 投递，不再依赖旧的手动绑定逻辑。
   - readiness 增加普通文件“可接收但未验证”的 warning，不再显示“未实现/未映射”。
4. 验证：
   - `python -m unittest tests.test_send_backends tests.test_bridge_send tests.test_send_readiness tests.test_send_bridge_worker` 通过，86 tests OK。
   - live Python bridge file probe 通过，native 后半链路在延迟后完整出现。
5. 仍需实机确认：
   - 当前仍不能把文件标记为 `delivery_verified=true`，需要用户在微信 UI 中确认 `codex_python_bridge_file_probe_20260709_201116.txt` 或后续 agent 文件确实出现在聊天底部。
   - 后续再做文件消息的 WeFlow/readback 验证，减少 accepted-unverified 长期停留。

### 2026-07-09 20:27 更新

1. 本轮利用防火墙关闭窗口完成了 outbox 真实链路复测：
   - `/QueryDB/status` 返回 `IsLogin=1`，native HTTP 端口在线。
   - send bridge worker 已重启到当前代码，进程 `22512`，配置签名仍为 `bridge_outbox + wechat_native_http`。
   - 文本探针 `bridge:filehelper:bde8e57a6e19` 已通过 WeFlow DB 回读验证为 `sent / wechat_native_http_send_text_verified`。
2. 修复并验证文件 outbox 路径：
   - 修复 `filehelper` 被 `_looks_like_wechat_receiver()` 误判为非法 receiver 的问题。
   - 修复文件入队和 native backend 直调时相对路径未规范化为绝对路径的问题。
   - 重启旧 worker 后，普通文件探针 `bridge:filehelper:f0b079f363ad` 进入 `accepted / wechat_native_http_send_file_accepted_unverified`，native response 包含 `ret=0 / native_result=1 / dispatcher_ready=true / dispatched=true`。
   - 诊断快照确认同一文件命中 `winapi_copy_file_w / sendfile_submit_factory / sendfile_task_factory / sendfile_high_entry / sendfileuploadmsg_init / sendfileuploadmsg_short`，且文件复制到 WeChat 缓存目录。
3. 文件 delivery verification 已从“待做”推进为可用：
   - `WeChatNativeHttpSendBackend.send_file()` 新增 WeFlow DB readback 验证。
   - 按发送前水位、outgoing、文件名、文件大小、时间/消息序号递增匹配 `<appmsg type=6>` 文件消息。
   - 实机文件探针 `bridge:filehelper:9babfac9e2b4` 已升级为 `sent / wechat_native_http_send_file_verified`，external id 指向 WeChat message DB 记录。
4. 验证：
   - `python -m unittest tests.test_send_backends tests.test_bridge_send tests.test_send_bridge_worker tests.test_send_readiness` 通过，90 tests OK。
   - `node --check app/personal_wechat_bot/ui/sidebar/app.js` 通过。
   - 当前 bridge `pending_count=0`，worker 心跳正常。
5. 残留：
   - 仍未做图片/GIF 真实发送，当前保持 unsupported。
   - 需要把 agent 自己生成的文件通道、对话 ledger 文件条目、记忆摘要/实体中的文件内容适配继续补齐。
   - 需要继续提升真实长对话聚合质量：区分用户/账号手动/agent 自己发送，避免历史逐条追答，按聊天底部当前状态组织回复。

### 2026-07-09 21:25 更新

1. 完成 agent 文本+文件多分段发送状态机加固：
   - `SendResult.details` 已承载 text/file 分段、bridge ids、part_count。
   - 确认队列新增结构化 `send_result / bridge_ids / bridge_acks / last_bridge_ack`，不再只靠 note 文本反查 bridge id。
   - `sync_bridge_ack_to_send_state()` 改为按所有分段 ack 聚合队列状态：任一分段未终结时整体保持 `queued_to_bridge`；全部 verified sent 后才进入 `sent`；accepted 仍保持未验证语义。
2. 修复任务栏过早完成/僵死风险：
   - send task metadata 记录所有 bridge ids。
   - bridge ack 同步会扫描 send task 的 structured metadata/detail/send_reason，并按分段 ack 聚合更新任务。
   - 文件先 ack、文本后 ack 时，任务保持 queued；全部分段终结后才 completed。
3. 发送审计增强：
   - `confirm_send_attempt` payload 包含完整 `send_result`，后续可以在前端/日志中看到 text/file 分段回执。
4. 新增回归：
   - `test_multi_part_bridge_ack_keeps_queue_and_task_open_until_all_parts_terminal` 覆盖 text+file 回复中文件先回执、文本后回执的真实顺序。
5. 验证：
   - `python -m unittest tests.test_reply_gate tests.test_conversation_ledger tests.test_send_commands tests.test_send_bridge_worker tests.test_bridge_send tests.test_send_backends`：160 tests OK。
   - `python -m unittest discover -s tests`：719 tests OK。
   - `node --check app/personal_wechat_bot/ui/sidebar/app.js`：OK。
   - 源码扫描 `app/scripts/tests` 未发现 WCF/wcferry/WeChatFerry 残留引用。
   - `git diff --check` 仅有既有 CRLF warning，无 whitespace error。
6. 残留：
   - 需要重启当前实机 send bridge worker/sidebar 相关进程，让本轮 Python 状态机改动进入运行态。
   - 下一步继续做 agent 自己生成文件的对话文件/记忆文件适配，以及真实长对话聚合质量。

### 2026-07-10 01:40 更新

1. 按《后续完整计划书.md》完成长任务闭环复核：
   - P0 运行安全：send bridge 单实例、worker lock/heartbeat/config matched、任务总台 stale worker 终态化、ack/queue/ledger/task 收敛、清历史保留 send_bridge 证据均已覆盖。
   - P1 对话正确性：补充群聊 owner 发言不进入 pending/participant 聚合、agent self echo 只确认 assistant 条目不重复接话的回归；既有多通道 scope、owner/self、失败未可见回复阻断、新私聊 greeting、连续 tick 不重复回复测试保持通过。
   - P2 文件和记忆：incoming 文件、outgoing agent 文件、file-only 回复、ledger send/readback 字段、memory Recent Files/entities 与 LLM 合并保真测试保持通过。
   - P3/H 前端与诊断：后端、CLI、HTTP API 已提供 `diagnostics-export`；前端诊断页新增“一键导出诊断”按钮，导出脱敏 JSON，并静态覆盖入口、脚本绑定和路由。
   - P4 扩展能力：图片/GIF/表情真实发送仍按计划保持 unsupported，不伪装 accepted/sent；普通文件和文本真实链路作为当前正式能力。

2. 本轮新增/确认的关键变更：
   - `app/personal_wechat_bot/control/sidebar_api.py`：诊断导出汇总 config、task、queue、audit、bridge、worker、native health、WeFlow、Agent、recent backend events，并脱敏 token/key/secret/password/authorization。
   - `app/personal_wechat_bot/control/sidebar_server.py`：`GET/POST /api/diagnostics/export`。
   - `app/personal_wechat_bot/control/cli.py`：`diagnostics-export --limit --no-persist`。
   - `app/personal_wechat_bot/ui/sidebar/index.html` 与 `app/personal_wechat_bot/ui/sidebar/app.js`：诊断页导出按钮、下载 JSON、状态提示。
   - `tests/test_sidebar_api.py`：诊断导出脱敏、群聊 owner 隔断、agent self echo 去重回归。
   - `tests/test_sidebar_server.py`：前端按钮和脚本绑定静态回归。

3. 验证命令：
   - `python -m unittest tests.test_sidebar_server.SidebarServerTest.test_sidebar_server_serves_state_and_index tests.test_sidebar_server.SidebarServerTest.test_sidebar_server_serves_dirty_state_script tests.test_sidebar_api.SidebarApiTest.test_diagnostics_export_captures_core_state_without_secrets -v`：3 tests OK。
   - `python -m unittest tests.test_sidebar_api.SidebarApiTest.test_sidebar_agent_snapshot_excludes_group_owner_messages_from_pending_and_participants tests.test_sidebar_api.SidebarApiTest.test_agent_self_echo_confirms_assistant_entry_without_repeat_reply -v`：2 tests OK。
   - `python -m unittest tests.test_sidebar_api tests.test_sidebar_server tests.test_conversation_ledger tests.test_memory_maintainer tests.test_channel_state_store -v`：183 tests OK。
   - `python -m unittest tests.test_send_bridge_worker tests.test_send_commands tests.test_task_manager tests.test_send_backends tests.test_send_readiness tests.test_bridge_send -v`：168 tests OK。
   - `python -m unittest discover -s tests`：759 tests OK。
   - `node --check app\personal_wechat_bot\ui\sidebar\app.js`：OK。
   - `python -m app.personal_wechat_bot.main --data-dir data diagnostics-export --limit 5 --no-persist`：OK，脱敏输出正常。
   - `python -m app.personal_wechat_bot.main --data-dir data send-driver-probe`：OK，`send_backend=wechat_native_http`，native `IsLogin=1`，worker `config_status=matched`，`pending_count=0`。
   - `git diff --check`：仅既有 CRLF warning，无 whitespace error。

4. 真实数据巡检：
   - task counts：`active=0`，`running=0`，`queued=0`，`waiting=0`，`blocked=0`。
   - confirm counts：`pending=0`，`approved=0`，`queued_to_bridge=0`，`accepted=0`，`sent=0`，`failed=3`，`rejected=0`。
   - bridge：`pending_count=0`，`active_unverified_count=0`。
   - worker：外部进程 `pid=2608`，`pid_alive=true`，`lock_alive=true`，`backend_name=wechat_native_http`，配置签名 `matched`。
   - native health：`http://127.0.0.1:30001/QueryDB/status` 在线，`IsLogin=1`。

5. 真实数据影响：
   - 本轮没有清历史，没有删除 `data/send_bridge`。
   - CLI 诊断导出使用 `--no-persist`；前端按钮默认会写入 `data/diagnostics/diagnostics-*.json` 并同时下载。
   - 保留证据仍为 `outbox.jsonl / acks.jsonl / synced_acks.json / accepted_reverify.json / .bridge_worker.lock`。

6. 当前完成定义：
   - 文本和普通文件非前台投递链路可用，文件 readback 已可验证 sent；图片/GIF/表情按计划保持 unsupported。
   - 总台、审核队列、bridge、ledger、memory、Agent 状态、前端展示、诊断导出均有测试或实机探针覆盖。
   - 当前没有 active/pending/approved/bridge backlog 残留，计划 A-I 主线已闭环。
